import React from 'react';
import classes from './SettingsIcon.module.css';

const SettingsIcon = () => {
    return (
        <div className={classes.settings_wrapper}>
          <span className={classes.settings_icon}  />
        </div>
    );
  };

  export default SettingsIcon;